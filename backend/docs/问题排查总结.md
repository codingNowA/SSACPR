# 问题排查总结

## 测试结果

### ✅ 独立测试通过
```bash
python test_llm_connection.py    # ✅ 成功
python test_extraction.py        # ✅ 成功
```

### ❌ API 请求失败
通过 FastAPI 接口调用时报错：
```
"LLM 调用失败: [Errno -5] No address associated with hostname"
```

## 问题原因分析

可能的原因：
1. **uvicorn 运行环境的 DNS 解析问题**
2. **httpx 客户端的连接池问题**
3. **异步上下文管理问题**

## 已尝试的修复

### 1. 配置 .env 加载
✅ 已修复 - 添加了 `load_dotenv()`

### 2. URL 标准化问题
✅ 已修复 - 避免重复添加 `/v1`

### 3. httpx 客户端配置
✅ 已添加 - `verify=True`, `follow_redirects=True`

## 🔧 建议的解决方案

### 方案 1: 重启服务（最简单）
```bash
# 停止当前服务 (Ctrl + C)
# 重新启动
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 方案 2: 检查网络环境
```bash
# 测试 DNS 解析
nslookup ws-na94bnwsg0gbbinn.cn-beijing.maas.aliyuncs.com

# 测试连接
curl -I https://ws-na94bnwsg0gbbinn.cn-beijing.maas.aliyuncs.com/compatible-mode/v1/chat/completions
```

### 方案 3: 使用测试脚本验证
```bash
# 先用测试脚本验证功能正常
python test_extraction.py

# 如果测试通过，说明代码没问题
# 问题在于 uvicorn 的运行环境
```

## 📝 下一步操作

1. **重启服务** - 让所有代码修改生效
2. **再次测试 API** - 使用 Swagger UI 或 curl
3. **如果还是失败** - 查看 uvicorn 的完整错误日志

## 🧪 测试 API

重启服务后：

```bash
# 方法 1: 使用浏览器
http://localhost:8000/docs

# 方法 2: 使用 curl
curl -X POST http://localhost:8000/api/v1/resume/extract \
  -F "file=@your_resume.pdf"
```

---

**诊断时间**: 2026-07-14  
**状态**: 代码修复完成，等待服务重启验证
